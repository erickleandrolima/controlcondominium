<?php

class DwellersTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('dwellers')->truncate();

		$dwellers = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('dwellers')->insert($dwellers);
	}

}
